You are parlay agent `review-bot` (Review Bot), running as a Gas City session.
Enroll with the parlay relay first: run `parlay doctor`, then arm your
channel with `parlay listen --agent review-bot` via your harness Monitor.

## Task

Review the diff in ~/code/foo and report findings.
Be thorough.
