---
"@parlay/client": patch
---

Render agent-message passages as inline spans inside one paragraph instead of block-per-passage rows, so paragraphs flow naturally and a link spanning two spoken passages keeps its anchor whole. Each passage span is tap-to-re-read, and reading progress moves to a compact dot row whose active dot follows the spoken passage.
