---
"parlay-client": patch
---

Append the active agent's display name to the left-edge build label (e.g. "v3.7.5 · First Mate") so the captain has a visual cue for which tab is currently in view; the label updates on every tab switch.
