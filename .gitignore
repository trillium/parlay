node_modules
dist
.env
packages/client/pulse-agent.js
packages/client/plugins/

# Build/runtime junk that kept leaking into git status (tree-hygiene, decision-u6v).
*.log
*.bak-*
packages/eval-engine/eval-engine
tools/relay/relay
interceptor-screenshot-*.png
packages/server/server.js
packages/server/src/parlay-ui.js
.claude-file-plan.json
.worktrees/
tools/cli/bin/

# Compiled Go binaries `go build ./...` drops in each module root. ~28 MB of
# these were committed once and still weigh on every clone; listed here so no
# module's output can be re-added. (parlay-bin also has its own .gitignore.)
tools/cli/cli
tools/parlay-bin/parlay-bin
tools/relay/relay
